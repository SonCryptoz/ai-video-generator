import type { Metadata } from "next";
import CreateNewPage from "./create-new";

export const metadata: Metadata = {
    title: "Create New Video - AI Video Creator",
    description: "Create a new AI-powered short video with custom settings.",
    icons: {
        icon: "/logo.svg",
    }
};

export default CreateNewPage;